<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        
        <!-- 
            Record Rules for odashboard.api.key
            
            Security model:
            - System admins (base.group_system) can see all API keys
            - Regular users can only see their own custom API keys
            - Default keys are only visible to admins (managed by ODashboard)
        -->
        
        <!-- Rule for regular users: can only see their own custom keys -->
        <record id="rule_odashboard_api_key_user_own" model="ir.rule">
            <field name="name">Users: own custom API keys only</field>
            <field name="model_id" ref="model_odashboard_api_key"/>
            <field name="domain_force">[
                '&amp;',
                ('user_id', '=', user.id),
                ('key_type', '=', 'custom')
            ]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="False"/>
            <field name="perm_create" eval="False"/>
            <field name="perm_unlink" eval="False"/>
        </record>
        
        <!-- Rule for system admins: full access to all keys -->
        <record id="rule_odashboard_api_key_admin_all" model="ir.rule">
            <field name="name">Admins: all API keys</field>
            <field name="model_id" ref="model_odashboard_api_key"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('base.group_system'))]"/>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>
        
    </data>
</odoo>
